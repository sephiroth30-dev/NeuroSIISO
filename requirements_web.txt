# Dependencias adicionales de la interfaz web de consulta.
# El resto del proyecto ya usa jinja2, pyodbc, python-dotenv y playwright
# (ver requirements.txt del proyecto en C:\NeuroficMigracion).
fastapi>=0.115
uvicorn>=0.30
